# 📹 Инструкция по загрузке видео

## Структура папки

Здесь хранятся видео для портфолио. Для каждого проекта нужно 3 версии:

### 1. **previewVideoUrl** - Маленькое превью (для списка)
- **Размер:** ~200-500 KB
- **Длительность:** 5-10 секунд (зацикленное)
- **Формат:** `.mp4` (H.264)
- **Разрешение:** 320x180 или 640x360
- **Без звука** (muted)
- **Имя файла:** `project-{id}-preview.mp4`
- **Пример:** `project-1-preview.mp4`

### 2. **hoverVideoUrl** - Превью для наведения (опционально)
- **Размер:** ~500 KB - 1 MB
- **Длительность:** 5-10 секунд (зацикленное)
- **Формат:** `.mp4` (H.264)
- **Разрешение:** 480x270 или 720x405
- **Без звука** (muted)
- **Имя файла:** `project-{id}-hover.mp4`
- **Пример:** `project-1-hover.mp4`
- ⚠️ Если не указано, используется `previewVideoUrl`

### 3. **fullVideoUrl** - Полное видео (для страницы проекта)
- **Размер:** может быть большим (2-50 MB)
- **Длительность:** полная версия ролика
- **Формат:** `.mp4` (H.264)
- **Разрешение:** 1920x1080 (Full HD) или выше
- **Со звуком** (если нужен)
- **Имя файла:** `project-{id}-full.mp4`
- **Пример:** `project-1-full.mp4`

---

## Как добавить видео в проект

### Вариант 1: Локальная папка (простой способ)

1. Загрузи видео в эту папку (`public/videos/`)
2. В файле `constants.ts` добавь ссылки:

```typescript
{
  id: '1',
  // ... другие поля
  previewVideoUrl: '/videos/project-1-preview.mp4',
  hoverVideoUrl: '/videos/project-1-hover.mp4', // опционально
  fullVideoUrl: '/videos/project-1-full.mp4',
}
```

### Вариант 2: Яндекс.Облако Object Storage (рекомендуется для продакшена)

1. Зарегистрируйся на [cloud.yandex.ru](https://cloud.yandex.ru)
2. Создай бакет в Object Storage
3. Загрузи видео через веб-интерфейс или API
4. Настрой публичный доступ
5. Получи прямые ссылки вида:
   ```
   https://storage.yandexcloud.net/vodichka-videos/project-1-preview.mp4
   ```
6. Вставь эти ссылки в `constants.ts`

---

## Рекомендации по оптимизации

### Для превью (previewVideoUrl):
```bash
# Используй ffmpeg для сжатия:
ffmpeg -i input.mp4 -vf "scale=640:360" -c:v libx264 -crf 28 -preset fast -an -t 10 -loop 1 output-preview.mp4
```

### Для hover (hoverVideoUrl):
```bash
ffmpeg -i input.mp4 -vf "scale=720:405" -c:v libx264 -crf 25 -preset fast -an -t 10 -loop 1 output-hover.mp4
```

### Для полного видео (fullVideoUrl):
```bash
ffmpeg -i input.mp4 -c:v libx264 -crf 23 -preset medium -c:a aac -b:a 128k output-full.mp4
```

---

## Пример структуры файлов

```
public/
  videos/
    project-1-preview.mp4  (маленькое превью)
    project-1-hover.mp4    (превью для наведения)
    project-1-full.mp4      (полное видео)
    project-2-preview.mp4
    project-2-hover.mp4
    project-2-full.mp4
    ...
```

---

## Важно!

- ✅ Все превью должны быть **зациклены** (loop)
- ✅ Превью должны быть **без звука** (muted)
- ✅ Используй формат **MP4 (H.264)** для максимальной совместимости
- ✅ Оптимизируй размер файлов для быстрой загрузки
- ⚠️ Если видео нет, будет использована картинка из `imageUrl`
